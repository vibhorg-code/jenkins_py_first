pipeline {
    agent any
    stages {
        stage('test') {
            steps {
                echo "Test app"
            }
        }
        stage('deploy') {
            steps {
                echo "Deploy app"
            }
        }
    }
}
